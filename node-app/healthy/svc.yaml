
apiVersion: v1
kind: Service
metadata:
  name: kubia
spec:
  #All request sent to the same Pod por a certain client
  #sessionAffinity: ClientIP
  ports:
  - port: 80
    targetPort: 9191
  selector:
    app: kubia