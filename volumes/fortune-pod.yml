apiVersion: v1
kind: Pod
metadata:
  name: fortune
spec:
  containers:
  - image: ualter/fortune
    name: html-generator
    imagePullPolicy: IfNotPresent
    volumeMounts:
    - name: html
      mountPath: /var/htdocs
  - image: nginx:alpine
    name: web-server
    volumeMounts:
    - name: html
      mountPath: /usr/share/nginx/html
      readOnly: true
    ports:
    - containerPort: 80
      protocol: TCP
  volumes:
  - name: html
    emptyDir: {}
